name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Parcel Scout
        run: |
          curl -s -o /dev/null -w "HTTP %{http_code}" \
            https://parcel-scout-dar2ookrtmvo6gnope3nxg.streamlit.app/
